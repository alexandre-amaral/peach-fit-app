# === APLICAÇÃO ===
APP_NAME="Peach Fit"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_TIMEZONE=America/Sao_Paulo
APP_URL=https://srv846765.hstgr.cloud

# === BANCO DE DADOS ===
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=srv846765_peach_fit
DB_USERNAME=srv846765_peach_user
DB_PASSWORD=

# === BROADCASTING (WebSockets) ===
BROADCAST_CONNECTION=pusher
PUSHER_APP_ID=peach-fit-app
PUSHER_APP_KEY=peach-fit-key
PUSHER_APP_SECRET=peach-fit-secret
PUSHER_HOST=srv846765.hstgr.cloud
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# === REVERB (Laravel Echo) ===
REVERB_APP_ID=123456
REVERB_APP_KEY=reverb-key
REVERB_APP_SECRET=reverb-secret
REVERB_HOST=srv846765.hstgr.cloud
REVERB_PORT=8080
REVERB_SCHEME=https

# === CACHE ===
CACHE_STORE=file
CACHE_PREFIX=peach_fit

# === SESSÃO ===
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=srv846765.hstgr.cloud

# === EMAIL ===
MAIL_MAILER=smtp
MAIL_HOST=smtp.hostinger.com
MAIL_PORT=587
MAIL_USERNAME=noreply@srv846765.hstgr.cloud
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@srv846765.hstgr.cloud"
MAIL_FROM_NAME="${APP_NAME}"

# === PAYPAL ===
PAYPAL_MODE=live
PAYPAL_LIVE_CLIENT_ID=
PAYPAL_LIVE_CLIENT_SECRET=
PAYPAL_SANDBOX_CLIENT_ID=
PAYPAL_SANDBOX_CLIENT_SECRET=
PAYPAL_CURRENCY=BRL

# === QUEUE ===
QUEUE_CONNECTION=database

# === LOGGING ===
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# === VITE ===
VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"